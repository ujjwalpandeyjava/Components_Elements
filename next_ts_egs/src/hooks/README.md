# What is hooks folder

The hooks folder contains custom React hooks used throughout the NextJS application.

Custom hooks encapsulate reusable logic and promote code reusability across different components.
